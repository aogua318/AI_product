# CEO 创业王修改器项目 — 工作规则

> 项目根目录：`D:\game\CEO`
> 本文是**强制规则 + 环境速查**。接续工作前先读本文，再按 §6 的入口读对应文档。

### 与全局规范的关系

- 全局规范见 `C:\Users\admin\.zcode\Agent.md`（ZCode 所有项目通用）。**本文（项目级）优先于全局规范**；两者冲突时以本文为准。
- 本文的修改须**先说明原因**再改动，并在 `v1.10移植实战记录.md` 末尾登记（参照"项目规则与文档体系更新 2026-09-14"条）。
- 全局规范中与本项目差异较大、需按项目覆盖的几条：
  - 全局建议"Windows 环境优先 PowerShell 7"；**本项目实际使用 Git Bash**（`bash` 工具），批量文件/字节操作优先用 `python` 脚本（本会话已多次使用），避免 PowerShell 转义踩坑。
  - 全局要求"默认只访问当前项目目录"；本项目**必须访问项目外资源**，清单见 §1.3（已按全局规定在此显式授权）

---

## 0.环境介绍

### 0.1 目的

本项目主要是对`D:\game\CEO`与`D:\gameapp\Steam\steamapps\common\創業王`这两个单机游戏项目进行逆向学习的工作，包括但不限于、逆向代码逻辑、功能分析、内容复刻、修改器制作与分析，功能扩展等。

其中`D:\game\CEO`下的版本是v1.02,`D:\gameapp\Steam\steamapps\common\創業王`下的版本是v1.10，当前以分析v1.10的内容为主。

### 0.2 工作规范

所有分析的发现的思路、遇到的错误与生成的成果结论都要及时写入文档，供用户或者其他环境的AI来迅速了解当前的情况与分析的过程。分析的产物在当前目录下新建文件夹分类放好。同类工作的内容文档跟资源在同一个目录下，比如与修改器相关的就在当前目录下的“CEO 修改器相关”文件夹里，逆向分析的结果在当前目录下的“CEO 逆向分析”的文件夹里。


### 0.3 工具提供（不同的环境目录可能不同，根据具体情况写）

当前环境下逆向的工具有

`D:\demoapp\IDA9.3` 已安装mcp工具
`D:\application\Cheat Engine7.5 zh` 已安装mcp工具

全局已经配置python

## 1. 目录与环境（2026-09-14 现状）

### 1.1 两棵工作树（注意：当前并存，以新树为准）

| 树 | 路径 | 状态 |
|---|---|---|
| **新树（权威）** | `D:\game\CEO\CEO 修改器相关\` | 用户 2026-09-13 起迁移到此；含全部文档、CE 数据、MCP 工具链、**1.10 反汇编资源** |
| 旧树（遗留） | `D:\game\CEO\修改器\` | 2026-09-13 前的副本，内容为旧树快照；**新工作不要写这里**，待合并 |

> 两棵树内容高度重复。除主表 CT 等产物外，**新产出统一写新树**。若发现两树文档不一致，以新树为准并顺手标注。

### 1.2 关键路径

| 项 | 路径 |
|---|---|
| 游戏本体（只读） | `D:\gameapp\Steam\steamapps\common\創業王\CEO.exe`（816,128 字节；32 位；**每次启动被重定位**） |
| Cheat Engine 7.51 | `D:\application\Cheat Engine7.5 zh\`（`autorun\ce_mcp_bridge.lua` 已放置，开机自动加载） |
| CE MCP 工具链 | `CEO 修改器相关\ce_mcp\`（ZCode 配置 `C:\Users\admin\.zcode\cli\config.json` 指向 `ce_mcp_server.js`，command=`node`） |
| **1.10 反汇编资源（ground truth）** | `CEO 修改器相关\v1.10反汇编资源\`：`04_IDA数据库\CEO_v1.10.exe.i64`（IDA 9.x 可直接打开）、`01_还原源码\`（2667 个函数伪 C，按模块分类）、`03_分析数据\`（functions/callgraph/strings/known_v110_final.json） |
| v1.02 规格书 | `CEO 修改器相关\创业王v1.02修改器分析\`（修改器逆向报告 + 10 段 AA 脚本 + 反编译产物）= 需求语义来源 |
| CE 数据产物 | `CEO 修改器相关\v1.10CE数据\`（**主表 `CEO_v1.10_主表.ct` 是权威交付物**） |
| 静态侦查小工具 | `CEO 修改器相关\v1.10移植\厂房侦查工具\`（`pe_aob.py` 通配 AOB 检索、`disasm.py` capstone 线性反汇编，纯静态不动进程） |
| IDA Pro 9.3 | `D:\demoapp\IDApro93\` |

### 1.3 项目外资源访问授权（按全局规范显式列出）

本项目是"逆向 + 内存修改"类工程，**必须**访问项目目录外的下列资源。除本清单外，不得访问其他项目外文件/服务。

| 资源 | 路径 | 访问方式（硬约束） |
|---|---|---|
| 游戏本体 | `D:\gameapp\Steam\steamapps\common\創業王\CEO.exe` | **只读**。可读取字节做静态检索、可附加进程做内存调试；**严禁修改/覆盖任何游戏文件**（所有修改只走运行时内存） |
| 存档目录 | 游戏目录下 `save\` | **只读**（仅用于观察"改动是否持久化"）；不得删改 |
| Cheat Engine | `D:\application\Cheat Engine7.5 zh\` | 可读写（含 `autorun\ce_mcp_bridge.lua` 的放置/更新）；改 CE 配置前告知用户 |
| IDA Pro 9.3 | `D:\demoapp\IDApro93\` | 只读调用（`ida.exe`/`idat.exe`）；IDA 工程产物写回项目树 |
| ZCode 配置 | `C:\Users\admin\.zcode\cli\config.json`（MCP 服务器配置） | 可读写；改动前先备份原内容并在回复中展示 diff |
| 全局规范 | `C:\Users\admin\.zcode\Agent.md` | 只读 |
| v1.02 语料/旧资源（若存在） | `D:\1game\CEO_chs\`、`E:\demo\gitclone\AI_product\` | **当本机不存在时不要去创建/寻找**，用项目内的 `v1.10反汇编资源\` 替代 |

> 注：`D:\game\CEO\CEO 修改器相关\` 虽在项目根之下，但其中的 `v1.10反汇编资源\05_资源解包\`（105MB 游戏资源解包）属**派生数据**，只读引用，不要在其中做修改。

### 1.4 GitHub 仓库与发布流程（2026-09-14 起）

| 项 | 内容 |
|---|---|
| 仓库 | `https://github.com/aogua318/AI_product.git`（**公开仓库**） |
| 本地克隆 | `E:\Demo\github\AI_product`（顶层还含 `MD5/`、`kubejs_and_create/` 等无关项目） |
| 目录映射 | 工作区 `D:\game\CEO\CEO 修改器相关\` ↔ 仓库 `AI_product\CEO 修改器相关\`；项目规范以 `CEO 修改器相关\AGENTS.md` 入库 |
| 授权状态 | 用户 2026-09-14 明确授权提交与推送；**但每次推送前仍须先说明提交范围**（公开仓库 = 对外发布，推出去可能被缓存/索引） |
| 有意不入库 | `v1.10反汇编资源\05_资源解包\`（105MB 游戏美术/音频素材：可再生 + 版权风险 + 单文件 59MB 超阈值）→ 见仓库根 `.gitignore` 与资源包 README 说明 |

**同步流程**：改工作区文件 → 复制到克隆对应路径（排除 `05_资源解包`）→ `git add -A` → 中文提交说明 → `git push origin main`。

**已知操作坑**：
- 2698 个文件的 `git add` 可能**超过 2 分钟**，务必等它结束；若被中断会留下 `.git/index.lock`，**确认无 git 进程后再删锁**，不要贸然删除；
- 推送报 `Connection reset / HTTP 408` 是**代理**导致（本机装了 Clash Verge）。**2026-09-15 实测更正了旧建议**（旧建议写的 `http.postBuffer=524288000` 是错的，见下）：
  1. **不要直连**：Clash 的 TUN 模式会掐断 git 的 HTTPS（`fetch`/`ls-remote` 报 `Failed to connect to github.com:443`，但 `ping` 与裸 TCP 443 测试都通，容易误判为"网络没问题"）；
  2. **显式走本地代理端口**：`git -c http.proxy=http://127.0.0.1:7897 <命令>`（端口号取自 Clash Verge 的"混合端口"，可在其设置里核对；`netstat -ano | grep 7897` 可确认在监听）。小请求（`ls-remote`/`fetch`）这样立刻可用；
  3. **`http.postBuffer` 必须调小、不是调大**：设成 512MB 会让 git 放弃分块传输、改用单个大请求，反而被代理判超时（HTTP 408）。实测 `http.postBuffer=1048576`（1MB，强制分块）+ 走代理，5MB 的推送**一次成功**；
  4. 完整可用命令：`git -c http.proxy=http://127.0.0.1:7897 -c http.postBuffer=1048576 push origin main`；
  5. SSH 通道（`github.com:22` / `ssh.github.com:443`）实测**可达且不受代理干扰**，但本机 `~/.ssh` 为空、无密钥，若 HTTPS 长期不通可考虑配 SSH key（需用户在 GitHub 网页端添加公钥）。
- **先把两棵工作树的对应文件在本地核对一致再推**，避免把旧树的旧内容覆盖上去。核对时注意：本机 `core.autocrlf=true`，工作区与克隆的**换行符差异不算实质修改**——比对前先按 LF 归一，否则会把一批纯 CRLF 差异文件误当改动混进提交（实测每次约 23 个）。

---

## 2. 铁律（违反会浪费数小时，全部是踩过的坑）

### 2.1 地址记法：RVA 与 VA 必须分清 ⚠️ 最高频错误

项目同时存在两套体系：

- **CE / 表 / AA 脚本 / 本文档 → 一律用 `CEO.exe+RVA`**（模块内偏移，CE 自动按实际基址解析）。
- **二进制字节检索 / IDA / 伪代码 → 用 VA**，换算关系：**`VA = 0x400000 + RVA`**（例：金钱 `CEO.exe+D10C0` = VA `0x4D10C0`；放置类型 `CEO.exe+4A8C24` = VA `0x8A8C24`）。

**铁律**：跨工具检索地址前先换算。曾因把 RVA 当 VA 搜字节（搜 `24 8C 4A 00` 而非 `24 8C 8A 00`）得出"该全局无引用"的错误结论，险些误判已交付功能。文档中引用地址时**必须标注是哪一套**。

**⚠️ 第三套体系（2026-09-15 补充，坑 #18）**：**文件偏移 ≠ RVA**。PE 节的 `PointerToRawData` **不等于** `VirtualAddress`（本 exe `.text` 是 `0x400` vs `0x1000`，`.rdata` 是 `0xA0600` vs `0xA2000`）。用脚本扫 `CEO.exe` 字节拿到的是**文件偏移**，直接当 RVA/VA 写进产物会全错（实测 92 条中文串地址全部偏移 `0xC00`～`0x1C00`，拿去 CE 查会全查不到）。
换算：`RVA = VirtAddr + (文件偏移 − RawPtr)`；且**必须用独立来源交叉验证**（本次用 `03_分析数据\strings_all.json` 的 VA 记法比对，696/696 一致才定案）。凡是跨"文件 ↔ 内存"边界的地址，先做节表换算。

### 2.2 禁止硬编码绝对地址

进程每次启动基址都变（实测如 0x2F0000）。所有脚本/表项/伪代码一律 `CEO.exe+偏移`。反汇编核对时也要检查注入后 newmem 里立即数是否等于本次运行的真实地址。

### 2.3 不要盲写内存

游戏有内部一致性校验，盲写未初始化结构会闪退（实测：对空地盲写矿物三件套 → 游戏卡死重启）。
**优先走游戏原生函数/原生校验路径**（1.10 反汇编资源里能找到游戏自己的调用范例）；任何新写入功能**先单点验证，再批量**；改前记录原值，测后还原现场。

### 2.4 CT 文件是 XML

脚本注释里**不能出现裸 `<`、`&`**（CE 会报 `Name starts with invalid character` 并拒绝加载整表）。改完 CT 必须先自检：

```bash
python -c "import xml.etree.ElementTree as ET; ET.parse(r'<ct路径>'); print('XML OK')"
```

### 2.5 CE AA 语法

十六进制**不带 `h` 后缀**（写 `4D10B8` 而不是 `4D10B8h`，否则汇编失败且勾选无反应、无报错）。脚本改完先 `ce_check_assemble` 校验，注入优先走 `ce_execute_lua(pcall(autoAssemble, ...))` 通道（MCP 的 `ce_auto_assemble` 偶发 `failed: nil`），注入后必须 `ce_disassemble` 复核。

### 2.6 用户资产与不可逆操作

- **本项目不是 git 仓库**（无版本控制）→ **文档就是唯一的历史记录**：这正是 §3 强制落档的根本原因，任何"改动前原值/改动后结果/如何还原"都要落到文档里，否则无法追溯。
- 不得擅自 `git init` / 提交 / 推送；不得引入版本控制类操作改变用户目录状态。
- **先看再动**：删除或覆盖任何文件前，先读它的内容；若发现内容与用户描述不符、或该文件非本次会话产出，**先报告再行动**，不要直接覆盖。
- 测试痕迹必须还原：改内存前记录原值，测完还原现场（本会话每次实测后都要求"已还原"并记入文档）。
- 破坏性/不可逆操作（软件重装、删除工程目录、清空游戏存档、改动 CE 安装目录）必须先经用户确认。
- 不擅自安装工具：需要新工具（IDA 插件、Python 库等）时先告知用户并询问由谁安装。

---

## 3. 过程文档化（强制）

**每个任务（一个功能点 / 一次调试会话 / 一次工具链变更 / 一次核验）完成后，必须写档再收尾**，不许只做不说。

### 3.1 文档落点

| 内容 | 落点 |
|---|---|
| 功能开发过程 | `v1.10移植\v1.10移植实战记录.md` 追加一章（步骤 + 难点坑点 + 实测定案，沿用"现象→根因→解法"格式） |
| 里程碑推进 | `v1.10移植\v1.10新修改器开发计划.md`（打勾 + 定案记录） |
| CE 数据产物（CT 表/脚本/地址表） | `v1.10CE数据\`，并在实战记录的产物清单登记状态 |
| 长任务/跨会话任务 | 单独建交接文档（参照 `厂房自动分配_侦查进度.md`）：目标、已实锤发现、已排除死路、候选线索、下一步、当前环境状态 |
| 方案核验 | 单独建核验报告（参照 `厂房自动分配_可行性评估_核验报告.md`）：逐条核对 + 修订清单 + 新增地址 |
| 坑点 | 统一补进实战记录 §3 坑点清单（已累计 #1-#12），编号连续 |

### 3.2 文档质量要求

1. **可复现**：最终地址一律 `CEO.exe+RVA`；脚本记录全文或指向 CT 文件与具体条目；定位方法写清工具调用序列与判据。
2. **证据分级**：区分"实锤"（断点命中 / 字节唯一命中 / 伪代码直读 / 实测行为）与"推测"，标注置信度；不确定的写进"待验证清单"，不要伪装成结论。
3. **坑按"现象→根因→解法"写**。
4. **方法比结论更重要**：地址会随版本/重定位失效，定位方法（值扫描链、差分实验、常驻断点、伪代码核对）不会——两者分开写。

---

## 4. 工作方法（新功能的标准流程）

1. **规格**：先从 v1.02 修改器分析报告 / 反编译产物拿到该功能的语义（钩什么、改什么、怎么判），**注意 v1.02 的转述可能有误，要实测校正**。
2. **静态定位（首选，先做）**：在 `v1.10反汇编资源\01_还原源码\` 里直接查伪代码，或用 `厂房侦查工具\pe_aob.py` / `disasm.py` 做字节检索。**能读代码就别猜内存**。
3. **活进程验证**：CE MCP 附加游戏 → 硬件断点抓现场寄存器（比 `ce_find_what_writes` 的监控窗口可靠，不依赖操作时机）→ `ce_get_set_context` 读现场。
4. **单点实测**：先只改一个目标、验证游戏侧表现（UI 数字/贴图/存档持久性），确认后再批量。
5. **批量与封装**：优先"纯 Lua + 符号寻址"（免疫重定位），必要时才用 AA 钩子；带 0/-1 之类的"不修改"语义时注意与合法值（如矿物 0=金）冲突。
6. **收尾写档**（见 §3），并把新踩的坑编号追加。

### 4.1 交互/验证注意事项

- 需要用户配合的操作（放工人、点机器、拾取等）**要给出明确、可执行的指令**，并说明预期现象与"没反应时怎么办"。
- CE 组合键热键可能与游戏内置快捷键冲突（Ctrl+C 已踩），选键前先查游戏快捷键。
- 游戏重启后旧补丁全部失效：必须**重新勾选 CT 脚本条目**；MCP 也需要重新附加进程。
- 选中对象统一入口（F3 及后续功能复用）：`元素 = [CEO.exe+C43DC] + [CEO.exe+C43C8] × [CEO.exe+B1024]`（编号/步长/基址三全局）。

### 4.2 沟通与验证规范

- **如实报告**：测试/注入失败、现象不符预期时必须明确说出，不得隐瞒或用"应该已经生效"之类措辞掩盖；**不得声称执行了实际没执行的操作**（全局规范要求，本项目尤甚——内存修改的假阳性代价很高）。
- **结论必须带证据**：区分"实锤"（断点命中 / 字节唯一命中 / 伪代码直读 / 游戏内实测）与"推测"；推测要显式标注并列入"待验证清单"（见 §3.2）。
- **给建议要说明理由**；存在多个可行方案（如数据直写 vs 调原生函数 vs 模拟点击）时，列出各自优缺点与风险，**让用户决定**，不要替用户拍板；对可以安全判断的小问题直接处理，不因无关细节频繁询问。
- **需要用户配合时**：给出明确、可执行的步骤（点哪个按钮、看哪个数字、预期现象是什么、没反应时怎么办），一次只要求一步，并在收到反馈后再继续。
- **涉及用户资产或破坏性操作前先确认**（见 §2.6）。

---

## 5. 环境故障速查

| 症状 | 处理 |
|---|---|
| `ce_ping` 反复超时；CE Lua 报 `Failed to create pipe: ce_mcp_bridge` | 残留 `node ce_mcp_server.js` 进程占着旧管道：任务管理器杀干净 → **重启 CE**（autorun 重建管道）→ 重启 ZCode 会话 |
| MCP 工具在会话里不存在 | 检查 `C:\Users\admin\.zcode\cli\config.json` 指向 `ce_mcp\ce_mcp_server.js` 且 `command=node`；**MCP 只在会话启动时连接**，改完要重开会话。不要用 `npx -y cheatengine@latest`（联网拉包易静默失败） |
| 附加后模块枚举为空、报"无法确定 CEO.exe 的含义" | 目标进程已变（游戏重启过）：重新 `ce_attach_process("CEO.exe")`；仍空则 `ce_symbol_control(operation="load_new")` |
| 断点设了不命中 | 该地址在当前上下文不被访问：改用"让目标被持续读取"的场景（打开信息窗口），或换到调用点/上层函数下断 |
| 游戏卡死 | `ce_cleanup()` 清断点；必要时 `ce_debug_continue(method="run")` 放行 |
| 扫描会话占满 | 最多 5 个并发，`ce_scan_list` 查看后 `ce_scan_close` 释放；注意 `ce_cleanup` 会连扫描会话一起清 |

---

## 6. 接续工作入口（会话重启后按需读）

| 目的 | 先读 | 再读 |
|---|---|---|
| **了解项目全貌 / 决定下一步做什么** | `v1.10移植\v1.10修改器_成果总览与后续路线.md`（一页看全） | 需要细节时再按下面几行深入 |
| 继续做已实现功能的维护/回归 | 本文 §1-2 | `v1.10移植实战记录.md` §0（功能总览 + 关键地址总表 + 主表使用说明） |
| 开发下一个新功能 | 本文 §4 | `v1.10新修改器开发计划.md`（阶段清单与定案）→ `v1.10反汇编资源\` 查代码 |
| 接续"厂房自动分配"任务 | `厂房自动分配_侦查进度.md`（侦查成果 + 下一步） | `厂房自动分配_可行性评估.md` + **其核验报告** | 已由游戏原生控制台 `wkrfill` 解决（见实战记录"重大发现"章），无需开发 |
| 核验他人/他 agent 的结论 | 本文 §3.2 | `v1.10反汇编资源\`（伪代码为 ground truth）+ `pe_aob.py` 字节检索 |

---

## 7. 当前进度快照（2026-09-14）

- **已交付**：主表 `CEO_v1.10CE数据\CEO_v1.10_主表.ct` —— F2 无限金钱、F3 矿物/品质/储量修改（含野生矿地）、Ctrl+Z 主管 / Ctrl+X 工人 / Ctrl+V 锁宣传车；全部 CE GUI 实测通过。
- **★ 游戏内置作弊控制台（2026-09-14 发现，重要）**：**按住左 Ctrl 按 F5** 打开，**ESC** 关闭；命令（经三重穷尽验证，全游戏仅 7 条）：`wkrfill`（一键配满当前厂房各岗位人力）、`buildok`（当前选中物件一键完工）、`buildokall`（全部完工）、`money`/`money1`（+1 亿）、`talk`/`notalk`。主表已用"模拟按键 + 窗口消息"把常用命令绑成 **F6/F7/F8/F9/F10** 热键（不调游戏函数、不要求游戏在前台）。定位方法与自动化技术见实战记录"重大发现"章，结论汇总见 `v1.10修改器_成果总览与后续路线.md`。
  → **阶段 3.5 厂房自动分配 = 由 `wkrfill` 原生覆盖，无需开发**；阶段 5 的"秒建设"由 `buildok`/`buildokall` 覆盖（`buildok` 已实测）。
- **待开发**：F4 强占厂房 + Alt 购地、机器秒生产、打包为独立修改器、各钩点 AOB 特征码自定位。
- **未解疑点**：直接调用游戏函数（`sub_426BF0` / `sub_45A8D0`）会导致游戏崩溃（坑 #13/#14）——已改走"用原生控制台 / 只读读数据"的路线，不再盲目调函数。
