# 05 - 疑问清单

看源码时不懂的问题记在这里。**弄懂之后在问题下补一行答案（附文档链接），并把 `[ ]` 改成 `[x]`。**

## 待解决

- [ ] kubejs 的 `interfaces.json` 到底怎么被用？`interfaceInjectionData(...)` 依赖在里面起什么作用？（README 提到但没细讲）
- [ ] `kubejs.classfilter.txt` 的语法和作用范围？
- [ ] Rhizo / Rhino 中 `registerTypeWrappers` 转换的具体规则：JS 传一个字符串时，怎么知道该转成哪个类？（入口：`KubeCreateOutput::wrapProcessingOutput`）
- [ ] schema JSON 里 `"merge": {"keys": true}` 的完整含义
- [ ] `EventGroup` 的 `startup/server/client/network` 四种事件在底层如何分发？
- [ ] 两个仓库 MC 版本不同（26.1.2 vs 1.21.1），KubeJS-Create 要升级到新版本需要改哪些地方？
- [x] `ProcessingOutputMixin` 具体混入了什么、为什么作者说是 "mildly cursed"？
  **答**：接口注入型 mixin，类体为空，只让 Create 的 `ProcessingOutput` 额外实现 `KubeCreateOutput` 接口，从而能被注册为 JS 包装目标。"cursed" 指让别人的类实现自己接口这种 hack 写法。见 [06 文档末尾](06-不装KubeJS-Create能替代多少.md)。
- [ ] kubejs 源码里 `typings/` 包是干什么的——是不是生成 `.d.ts` 给 VSCode 补全用？

## 已解决

（还没有）
